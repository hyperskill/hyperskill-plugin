name: Publish to JetBrains Marketplace

on:
  push:

concurrency:
  group: publish-plugin-${{ github.ref_name }}
  cancel-in-progress: true


jobs:
  versions:
    name: Support Versions
    runs-on: arc-runners-small
    timeout-minutes: 60
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Generate matrix from printProductsReleases
        id: set-matrix
        run: |
          RAW=(
            "2025.1",
            "2025.2"
          )
          
          JSON=$(echo "$RAW" | sort | uniq | jq -R . | jq -s -c '{version: .}')
          echo "Versions: $RAW"
          echo "matrix=$JSON" >> $GITHUB_OUTPUT
  
  test:
    name: Run Tests for ${{ matrix.version }}
    needs: versions
    runs-on: arc-runners-small
    timeout-minutes: 60
    strategy:
      matrix: ${{ fromJson(needs.versions.outputs.matrix) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version-file: '.java-version'
          distribution: 'corretto'

      - name: Run Tests
        run: |
          MAJOR=$(echo "$VERSION" | cut -d '.' -f1)
          if [[ "$MAJOR" =~ ^20[2-9][0-9]$ ]]; then
          MINOR=$(echo "$VERSION" | cut -d '.' -f2)
          environmentName=$(( (MAJOR - 2000) * 2 + MINOR - 1 ))
          else
          environmentName="$MAJOR"
          fi
          echo "environmentName=$environmentName"
          ./gradlew test --no-daemon -PenvironmentName=$environmentName
        env:
          PLATFORM_VERSION: ${{ matrix.version }}
          COMPATIBLE_PLUGINS: ''  # FIXME: It's fix for EAP version IDE

  verify:
    name: Verify Plugin for ${{ matrix.version }}
    runs-on: arc-runners-small
    timeout-minutes: 60
    strategy:
      matrix: ${{ fromJson(needs.versions.outputs.matrix) }}
      fail-fast: false
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version-file: '.java-version'
          distribution: 'corretto'

      - name: Verify Plugin
        run: |
          MAJOR=$(echo "$VERSION" | cut -d '.' -f1)
          if [[ "$MAJOR" =~ ^20[2-9][0-9]$ ]]; then
          MINOR=$(echo "$VERSION" | cut -d '.' -f2)
          environmentName=$(( (MAJOR - 2000) * 2 + MINOR - 1 ))
          else
          environmentName="$MAJOR"
          fi
          echo "environmentName=$environmentName"
          ./gradlew verifyPlugin --no-daemon -PenvironmentName=$environmentName

#  publish:
#    name: Publish Plugin
#    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
#    runs-on: arc-runners-small
#    needs:
#      - test
#      - verify
#    timeout-minutes: 30
#    steps:
#      - uses: actions/checkout@v4
#
#      - uses: actions/setup-java@v4
#        with:
#          java-version-file: '.java-version'
#          distribution: 'corretto'
#
#      - name: Publish Plugin
#        run: |
#          ./gradlew publishPlugin --no-daemon
#        env:
#          JB_MARKETPLACE_TOKEN: ${{ secrets.JB_MARKETPLACE_TOKEN }}
